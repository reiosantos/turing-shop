import { VcProductAttribute } from '@turing/shared/models/vc-product-attribute';

export class VcAttribute {
  attributeId: number;
  attributeValueId: number;
  value: string;
  productAttribute: VcProductAttribute;
}
